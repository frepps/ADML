import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
  // Language features are declaratively contributed via package.json.
  // Future enhancements (diagnostics, formatting, hover) can be added here.
}

export function deactivate() {}
